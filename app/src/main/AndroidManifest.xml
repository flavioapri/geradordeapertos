<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.flavio.geradordeapertos">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme">
        <activity android:name=".RelacaoProgramas">

        </activity>
        <activity
            android:name=".CadastroPrograma"
            android:label="@string/cadastro_programa"></activity>
        <activity
            android:name=".CadastroMotivo"
            android:label="@string/cadastro_motivo" />
        <activity
            android:name=".CadastroApertadeira"
            android:label="@string/cadastro_processo"></activity>

        <activity
            android:name=".GeradorRegistroGrupo"
            android:label="@string/gerar_registro_grupo" />
        <activity
            android:name=".Leitura"
            android:label="@string/leitura" />
        <activity
            android:name=".Sobre"
            android:label="@string/sobre" />
        <activity
            android:name=".Configuracoes"
            android:label="@string/configuracoes" />
        <activity
            android:name=".Historico"
            android:label="@string/historico" />
        <activity
            android:name=".GeradorRegistro"
            android:label="@string/gera_registro"></activity>
        <activity
            android:name=".Main"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>